# Политика безопасности — Tetris CLI

## 📖 Содержание

- [Поддерживаемые версии](#поддерживаемые-версии)
- [Сообщение об уязвимостях](#сообщение-об-уязвимостях)
- [Процесс исправления](#процесс-исправления)
- [Безопасность в проекте](#безопасность-в-проекте)
- [Рекомендации пользователям](#рекомендации-пользователям)

---

## 📋 Поддерживаемые версии

В настоящее время поддерживается только последняя версия:

| Версия | Поддержка | Статус |
|--------|-----------|--------|
| 23.96.x | ✅ Полная | Текущая |
| 23.95.x | ❌ Нет | Устарела |
| < 23.95 | ❌ Нет | Устарела |

**Рекомендация:** Всегда используйте последнюю версию из репозитория.

---

## 🚨 Сообщение об уязвимостях

### Критические уязвимости

Для сообщений о критических уязвимостях (безопасность, потеря данных):

**Вариант 1: GitHub Private Vulnerability Reporting**

Используйте [GitHub Security Advisories](https://github.com/Githab-capibara/tetris-cli/security/advisories) для создания приватного отчёта.

**Вариант 2: Email**

Отправьте описание уязвимости на email (если указан в профиле мейнтейнера).

**Вариант 3: GitHub Issues (не рекомендуется)**

Создайте issue с пометкой **[SECURITY]** в заголовке, но **не раскрывайте детали уязвимости** в публичном доступе.

### Что включать в отчёт

**Минимальная информация:**
```markdown
### Тип уязвимости
[Описание типа уязвимости]

### Затронутые версии
[Версии, которые подвержены уязвимости]

### Воспроизведение
[Шаги для воспроизведения (приватно)]

### Возможные последствия
[Что может произойти при эксплуатации]

### Предлагаемое решение (если есть)
[Ваши предложения по исправлению]
```

**Дополнительно:**
- Скриншоты/логи (приватно)
- Доказательство концепции (PoC)
- Ссылки на похожие уязвимости (CVE)

### Время ответа

- **Критические**: 24-48 часов
- **Высокие**: 3-5 дней
- **Средние**: 1-2 недели
- **Низкие**: 2-4 недели

---

## 🔧 Процесс исправления

### Этапы процесса

```
1. Получение отчёта
         ↓
2. Подтверждение уязвимости
         ↓
3. Оценка критичности
         ↓
4. Разработка исправления
         ↓
5. Тестирование исправления
         ↓
6. Публикация Security Advisory
         ↓
7. Выпуск патча
         ↓
8. Публичное раскрытие (через 30 дней)
```

### Роли и ответственность

**Мейнтейнер:**
- Получение и проверка отчётов
- Координация исправления
- Публикация advisories
- Выпуск патчей

**Контрибьюторы:**
- Помощь в исправлении (по приглашению)
- Code review исправлений
- Тестирование патчей

### Политика раскрытия информации

1. **До публикации**: Информация об уязвимости остаётся приватной
2. **При публикации**: Security Advisory становится публичным
3. **Через 30 дней**: Детальное описание уязвимости (если безопасно)

---

## 🛡️ Безопасность в проекте

### Текущие меры безопасности

#### 1. Защита рекордов

**BLAKE3 хеширование с солью:**
```rust
// Генерация криптографически стойкой соли
let salt = get_random_hash(); // 32 байта случайных данных

// Хеширование с солью
let hash = blake3::hash(&format!("{}{}", salt, score));

// Проверка целостности
assert!(saved_hash == computed_hash);
```

**Защита от подделки:**
- Соль генерируется через `rand::thread_rng()` (криптостойкий)
- BLAKE3 — криптографически стойкая хеш-функция
- Проверка при каждой загрузке

#### 2. Валидация входных данных

**Имена в таблице лидеров:**
```rust
// Фильтрация символов
let validated: String = trimmed
    .chars()
    .filter(|&c| is_valid_name_char(c)) // Только безопасные символы
    .take(20) // Ограничение длины
    .collect();
```

**Разрешённые символы:**
- Буквы (a-z, A-Z, Unicode)
- Цифры (0-9)
- Специальные: `_`, `-`, ` `, `.`

#### 3. Path Traversal защита

**Валидация путей в `controls.rs`:**
```rust
// Проверка на абсолютные пути
if path.starts_with('/') {
    return Err("Абсолютные пути запрещены".into());
}

// Проверка на path traversal (..)
if path.components().any(|c| c.as_os_str() == "..") {
    return Err("Path traversal запрещён".into());
}

// Сохранение только относительных путей
File::create(&path)?;
```

**Запрещено:**
- Абсолютные пути (начинаются с `/`)
- Path traversal (`..`)
- Символические ссылки на файлы вне директории проекта

#### 4. Безопасная работа с памятью

**Гарантии Rust:**
- Нет висячих указателей
- Нет переполнений буфера
- Нет data races
- Автоматическое управление памятью

#### 4. Обработка ошибок

**Правильная обработка:**
```rust
// Вместо unwrap() используется обработка ошибок
match load_config() {
    Ok(data) => data,
    Err(e) => {
        eprintln!("Предупреждение: {}", e);
        Self::default()
    }
}
```

### Аудит безопасности

**Последний аудит:** Март 2026

**Результаты:**
- 🔴 Критических уязвимостей: 0
- 🟠 Высоких уязвимостей: 0
- 🟡 Средних уязвимостей: 1 (исправлена)
- 🟢 Низких уязвимостей: 2 (приняты)

**Отчёт:** [AUDIT_REPORT.md](AUDIT_REPORT.md)

---

## 👤 Рекомендации пользователям

### Безопасное использование

#### 1. Установка из проверенных источников

```bash
# ✅ Рекомендуется: установка через cargo
cargo install tetris-cli

# ✅ Альтернатива: сборка из исходников
git clone https://github.com/Githab-capibara/tetris-cli.git
cd tetris-cli
cargo build --release

# ⚠️ Осторожно: сторонние пакеты
# Проверяйте источник пакета перед установкой
```

#### 2. Проверка целостности

```bash
# Проверка хеша бинарника (если доступен)
sha256sum tetris-cli

# Сверка с опубликованным хешем в релизе
```

#### 3. Права доступа

**Рекомендуемые права для файлов конфигурации:**
```bash
# Ограничить доступ к файлам с рекордами
chmod 600 ~/.config/tetris-cli/config.toml
```

#### 4. Обновления

**Регулярно обновляйтесь:**
```bash
# Проверка обновлений
cargo install --force tetris-cli

# Или через git
git pull origin main
cargo build --release
```

### Что делать при подозрении на уязвимость

1. **Немедленно сообщите** мейнтейнеру
2. **Не раскрывайте** детали публично
3. **Дождитесь патча** перед обновлением
4. **Проверьте** файлы конфигурации после обновления

---

## 📞 Контакты

### Экстренная связь

Для срочных вопросов безопасности:

- **GitHub Security Advisories**: [Создать advisory](https://github.com/Githab-capibara/tetris-cli/security/advisories/new)
- **Email**: (указан в профиле мейнтейнера)

### Обычные вопросы

- **GitHub Issues**: [Создать issue](https://github.com/Githab-capibara/tetris-cli/issues) с меткой `security`
- **Discussions**: [Обсуждение безопасности](https://github.com/Githab-capibara/tetris-cli/discussions)

---

## 📜 История безопасности

### 2026

**Март 2026** — Аудит безопасности
- Проверка всех компонентов
- Исправление 1 средней уязвимости
- Улучшение валидации имён

**Январь 2026** — Исправление уязвимости хеширования
- Обновление BLAKE3 до версии 1.5
- Улучшение генерации соли

### 2025

**Июнь 2025** — Первая политика безопасности
- Создание SECURITY.md
- Настройка GitHub Security Advisories

---

## 🙏 Благодарности

Благодарим исследователей безопасности за ответственную публикацию уязвимостей!

Все репортёры указываются в CHANGELOG (если желают).

---

**Последнее обновление:** 20 марта 2026
**Версия документа:** 1.1
