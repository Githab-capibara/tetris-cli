# Tetris CLI 🎮

Классическая игра Тетрис для терминала Linux/Unix, написанная на Rust.

![Скриншот игры](docs/img/screenshot.png)

## 📖 Описание

**Tetris CLI** — это полноценная реализация легендарной игры Тетрис, работающая прямо в вашем терминале. Игра поддерживает все классические механики: вращение фигур, мгновенное падение, подсчёт очков и сохранение рекордов.

### Особенности

- ✨ **Классический геймплей** — все 7 типов тетрамино (T, L, J, S, Z, O, I)
- 🎨 **Цветная графика** — каждая фигура имеет свой цвет
- 🏆 **Система рекордов** — лучший результат сохраняется между запусками
- ⚡ **Плавная анимация** — 60 FPS для комфортной игры
- 🎯 **Прогрессивная сложность** — скорость падения увеличивается с каждым уровнем
- ⏸️ **Пауза** — возможность приостановить игру в любой момент
- 👻 **Призрачная фигура** — показывает, куда упадёт фигура
- 👀 **Предпросмотр** — показ следующей фигуры
- 📊 **Таблица лидеров** — топ-5 лучших результатов
- 🔄 **Удержание фигуры (Hold)** — отложите фигуру для последующего использования
- 🔊 **Звуковые эффекты** — терминальный bell при удалении линий
- 📈 **Статистика игры** — подсчёт фигур, комбо, время игры
- 🏃 **Режим спринт** — очистите 40 линий как можно быстрее
- 🏃 **Режим марафон** — очистите 150 линий с нарастающей сложностью
- 🎮 **Настраиваемое управление** — возможность изменить все клавиши управления

## 🚀 Установка

### Через cargo (рекомендуется)

```bash
cargo install tetris-cli
```

### Из исходников

```bash
git clone https://github.com/Githab-capibara/tetris-cli.git
cd tetris-cli
cargo build --release
cargo run
```

### Пакетные менеджеры

- **Arch Linux (AUR)**: [tetris-cli-git](https://aur.archlinux.org/packages/tetris-cli-git)
- **NetBSD**: [tetris-cli](https://pkgsrc.se/games/tetris-cli)

## 🎮 Управление

### Клавиши по умолчанию

| Клавиша | Действие | Описание |
|---------|----------|----------|
| `a` | Влево | Перемещение фигуры влево |
| `d` | Вправо | Перемещение фигуры вправо |
| `q` | Вращение ↺ | Поворот против часовой стрелки |
| `e` | Вращение ↻ | Поворот по часовой стрелке |
| `w` | Hard Drop | Мгновенное падение (2 очка/ячейку) |
| `s` | Soft Drop | Ускоренное падение (1 очко/ячейку) |
| `c` | Hold | Удержать фигуру на потом |
| `p` | Pause | Пауза игры |
| `Backspace` | Выход | Вернуться в меню |

### Настройка управления

Все клавиши можно перенастроить через `ControlsConfig`:

```rust
use tetris_cli::controls::ControlsConfig;

// Загрузка сохранённой конфигурации
let config = ControlsConfig::load_from_file("my_controls.json").unwrap();

// Или создание новой (стиль Vim - HJKL)
let vim_config = ControlsConfig::custom(
    b'h', // влево
    b'l', // вправо
    b'j', // мягкое падение
    b'k', // жёсткое падение
    b'y', // вращение влево
    b'u', // вращение вправо
    b'i', // удержание
    b'o', // пауза
    127,  // выход
);
```

## 📋 Системные требования

- **ОС**: Linux или другая Unix-подобная система
- **Терминал**: Поддерживающий raw-режим и ANSI-цвета
- **Минимальный размер окна**: 22×25 символов
- **Шрифт**: Моноширинный (рекомендуется)

> ⚠️ **Важно**: Игра не работает в Windows Terminal. Для запуска в Windows используйте WSL (Windows Subsystem for Linux).

### Рекомендуемые терминалы

- GNOME Terminal
- Konsole
- Alacritty
- Kitty
- iTerm2 (macOS)

## 🎯 Правила игры

### Начисление очков

#### Базовые очки

| Действие | Формула | Пример |
|----------|---------|--------|
| Падение фигуры | 100 + (скорость × 50) | ~145 очков |
| Soft Drop | 1 × ячейки | 5 ячеек = 5 очков |
| Hard Drop | 2 × ячейки | 10 ячеек = 20 очков |

#### Очки за линии (с экспоненциальным бонусом)

| Линий | Формула | Очки | Название |
|-------|---------|------|----------|
| 1 | 100 × 2⁰ | 100 | Одиночный |
| 2 | 100 × 2¹ | 200 | Двойной |
| 3 | 100 × 2² | 400 | Тройной |
| 4 | 100 × 2³ + 1000 | 1800 | **TETRIS!** |

#### Бонусы

| Бонус | Условие | Очки |
|-------|---------|------|
| Комбо | 50 × (комбо - 1) | до 450+ |
| Уровень | 500 × (уровень - 1) | до 5000+ |
| Tetris | 4 линии одновременно | +1000 |

### Прогрессия сложности

- За каждую заполненную линию скорость падения увеличивается на 0.05
- Начальная скорость: 0.9 блоков/секунду
- Уровень повышается каждые 10 удалённых линий

| Уровень | Скорость | Линий для перехода |
|---------|----------|-------------------|
| 1 | 0.90 | 0-9 |
| 2 | 0.95 | 10-19 |
| 3 | 1.00 | 20-29 |
| 4 | 1.05 | 30-39 |
| 5 | 1.10 | 40-49 |
| ... | ... | ... |

### Режимы игры

#### Классический режим
- **Цель**: Набрать как можно больше очков
- **Конец игры**: При заполнении поля до верха
- **Сохранение**: Рекорд сохраняется в таблицу лидеров
- **Запуск**: Нажмите `Enter` в главном меню

#### Режим спринт
- **Цель**: Очистить 40 линий как можно быстрее
- **Конец игры**: При достижении 40 линий
- **Сохранение**: Только статистика, без рекордов
- **Запуск**: Нажмите `r` в главном меню

#### Режим марафон
- **Цель**: Очистить 150 линий
- **Конец игры**: При достижении 150 линий
- **Особенность**: Сложность растёт быстрее (каждые 5 линий)
- **Запуск**: Нажмите `m` в главном меню

### Проигрыш

Игра заканчивается, когда новая фигура не может появиться на поле (заблокирована другими фигурами).

## 🆕 Новые функции (текущая версия)

### 🔧 Критические исправления (версия 23.96.20+)

**C1: Безопасная конвертация f32 → u32:**
- Реализована явная проверка границ вместо `clamp()`
- Защита от NaN, Infinity, отрицательных значений и переполнения
- Добавлено 6 тестов на безопасную конвертацию

**C2: Устранение дублирования проверки коллизий:**
- Добавлен `VALID_X_RANGE: Range<i16>` для проверки границ
- Используется `Range::contains()` вместо множественных сравнений
- Добавлено 5 тестов проверки границ коллизий

**C3: Обработка ошибок в Application:**
- `Application::new()` возвращает `Result<Self, GameError>`
- `initialize_terminal()` использует `?` оператор с типизированными ошибками
- Улучшена обработка ошибок инициализации терминала

### ⚡ Оптимизации (версия 23.96.20+)

**H4: Документирование WALL_KICK_OFFSETS:**
- Добавлена таблица с описанием каждого смещения
- Улучшена читаемость кода вращения фигур

**H6: Оптимизация sanitize_player_name:**
- Используется `filter_map()` для объединения фильтрации
- Снижено количество аллокаций при валидации имён
- Добавлено 6 тестов валидации имён

**H7: Оптимизация validate_all:**
- Кэширование результата `canonicalize()`
- Снижено количество избыточных проверок путей
- Добавлено 5 тестов валидации путей

**M8: Оптимизация find_filled_lines:**
- `SmallVec<[usize; 4]>` вместо `Vec<usize>`
- Снижено количество аллокаций памяти при поиске линий
- Добавлено 6 тестов поиска заполненных линий

**M13: Проверка размера Tetromino:**
- Compile-time assert: `size_of::<Tetromino>() <= 40`
- Гарантия оптимального размера структуры
- Добавлено 4 теста размера структуры

**M22: Оптимизация can_move_curr_shape_direction:**
- `.any()` для раннего выхода при обнаружении коллизии
- Снижено количество избыточных проверок
- Добавлено 3 теста движения фигур

### 🔧 Критические исправления (версия 23.96.18+)

**C1: Безопасный cast в cycle.rs:**
- Добавлена безопасная конвертация `u128 → u64` в функциях `maintain_fps()` и `delta_time()`
- Защита от переполнения при длительных интервалах между кадрами
- Добавлены тесты на безопасность cast операций (3 теста)

**L1: Улучшена документация:**
- Добавлены backticks для имён функций, типов и констант в документации
- Улучшена читаемость документации API
- Исправлено форматирование примеров кода

**L2: Оптимизирован match в cycle.rs:**
- Объединены паттерны `match` для обработки `InputResult::Continue` и `InputResult::Pause`
- Упрощена логика обработки результатов ввода в игровом цикле
- Снижено количество дублирования кода

**L3: Улучшена обработка ошибок в application.rs:**
- Использован `if let` вместо `match` для обработки опционального рекорда
- Улучшена читаемость кода загрузки сохранений
- Добавлено логирование ошибок через `eprintln!`

**M4: Добавлены TODO комментарии:**
- Добавлены TODO комментарии для будущей рефакторизации трейтов в `cycle.rs`
- Константы с `#[allow(dead_code)]` для будущих улучшений
- Документированы планы по разделению модулей

### 🔧 Критические исправления (версия 23.96.16+)

**Защита от переполнения очков:**
- Все операции сложения очков заменены на `saturating_add()`
- Добавлены тесты на переполнение для u128
- Полная защита от переполнения в расчётах очков, комбо и бонусов

**Улучшение обработки ошибок:**
- Заменено игнорирование ошибок (`let _ =`) на логирование через `eprintln!`
- Обработаны ошибки в методах отрисовки
- Улучшена отказоустойчивость при сбоях терминала

**Документирование потокобезопасности:**
- Добавлена документация о потокобезопасности `LeaderboardEntry`
- Добавлен `#![deny(clippy::mut_mutex_lock)]`
- Добавлены атрибуты `#[must_use]` к методам таблицы лидеров

### ⚡ Оптимизации производительности

**Оптимизация аллокаций строк:**
- Используется `truncate(0)` вместо `clear()` для сохранения capacity
- Предварительное выделение памяти через `String::with_capacity()`
- Снижение количества аллокаций при отрисовке

### 🔧 Исправления именования

**Переименование поля `fall_spd` → `fall_speed`:**
- Улучшена читаемость кода
- Сохранена обратная совместимость через deprecated методы
- Обновлены все тесты и документация

### 📝 Best practices

**Удаление избыточных комментариев:**
- Удалены комментарии, дублирующие код
- Оставлены только объяснения "почему", а не "что"
- Улучшена читаемость кода

**Устранение дублирования кода:**
- Выделена функция `validate_config_path()` для валидации путей
- Используется `PathValidator::validate_all()` где возможно
- Соблюдение принципа DRY

### 🔧 Критические исправления

**Исправление экспорта GameStats:**
- Добавлен публичный экспорт `GameStats` для доступа к статистике игры
- Улучшена видимость структур для внешнего API

**Оптимизация импортов:**
- Исправлены недостающие импорты в модулях
- Улучшена организация модульной структуры

### ⚡ Оптимизации производительности

**Оптимизация sanitize_player_name:**
- Улучшена производительность валидации имён
- Снижено количество аллокаций при обработке Unicode

**Улучшение кэширования строк:**
- Расширено кэширование строк в игровом цикле
- Снижение количества аллокаций памяти при отрисовке

**Оптимизация проверок границ:**
- Улучшены проверки границ игрового поля
- Снижено количество избыточных проверок

**Оптимизация валидации Unicode:**
- Улучшена производительность whitelist-валидации
- Быстрая фильтрация разрешённых символов

### 🔧 Исправления логики

**Исправление обработки Direction::Down:**
- Унифицирована обработка направления вниз
- Улучшена согласованность с другими направлениями

### 📝 Best practices

**Добавление #[must_use] атрибутов:**
- Атрибуты добавлены ко всем методам, результат которых должен быть использован
- Улучшена безопасность API через компилятор

**Добавление #[track_caller]:**
- Добавлена трассировка вызовов для отладки
- Улучшено качество сообщений об ошибках

### 🏗️ Архитектурные улучшения (версия 23.96.14+)

**Разделение menu.rs на подмодули:**
- `menu/constants.rs` - константы меню
- `menu/draw.rs` - функции отрисовки (draw_menu, draw_leaderboard, draw_game_stats)
- `menu/input.rs` - обработка ввода (get_player_name, wait_for_key)
- Улучшено разделение ответственности, упрощение тестирования

**Terminal Backend абстракция:**
- Создан трейт `TerminalBackend` для кроссплатформенности
- Реализован `MockTerminal` для тестирования без терминала
- Добавлен `TerminalBackendExt` с методами по умолчанию
- Подготовка к поддержке Windows (crossterm)

**Улучшение документации:**
- Добавлена документация по безопасности (SECURITY.md)
- Централизация TODO комментариев в TODO.md
- Улучшена документация UTF-8 ограничений в KeyReader
- Добавлены примеры использования для публичных функций

**Оптимизация collision detection:**
- Выделена общая функция `check_block_collision()`
- Устранено дублирование кода между проверками коллизий
- Улучшена читаемость и поддерживаемость кода

### 🏗️ Архитектурные улучшения (версия 23.96.14+)

**Выделение RenderCache:**
- Создана структура `RenderCache` для кэширования строк отрисовки
- Уменьшено количество полей в `GameState` с 30+ до 24
- Улучшена организация кэширования: строки и последние значения в одной структуре

**Улучшение обработки ошибок:**
- Заменено игнорирование ошибок (`let _ =`) на логирование через `eprintln!`
- Обработаны ошибки в методах `Canvas::new_stub()` и `Canvas::draw_strs()`

**Улучшение потокобезопасности:**
- Добавлен маркер `PhantomData<*mut ()>` в `LeaderboardEntry`
- Явное указание на отсутствие `Send + Sync` для многопоточной безопасности
- Обновлена документация с предупреждением о TOCTOU

**Разделение ответственности в render.rs:**
- Выделены функции: `find_filled_lines()`, `animate_line_clear()`, `remove_lines()`, `update_score_for_lines()`
- Улучшена читаемость и тестируемость кода

**Оптимизация BagGenerator:**
- Удалено избыточное поле `is_filled: bool`
- Используется проверка `index >= bag.len()` для определения заполненности
- Уменьшено использование памяти на 1 байт на экземпляр

**Улучшение документации:**
- Добавлена секция `# Errors` ко всем методам трейтов `TerminalBackend` и `TerminalBackendExt`
- Добавлены комментарии с обоснованием значений для констант позиций
- Централизованы константы в `game/constants.rs`

**Улучшение валидации:**
- Полное делегирование валидации путей модулю `PathValidator`
- Удалено дублирование проверок в `controls.rs`

**Использование трейта GameBoardAccess:**
- Реализован трейт для `GameState`
- Добавлены методы `get_filled_lines()` и `set_filled_lines()`
- Уменьшена связанность между модулями

### 🧪 Тестирование

**1113 тестов** покрывают все аспекты игры:
- Комплексное тестирование игровой логики
- Тесты безопасности (HmacValidator, constant-time HMAC)
- Тесты оптимизаций производительности
- Валидация документации API
- Архитектурные тесты (отсутствие циклических зависимостей) — 19 тестов
- Тесты верификации исправлений
- Тесты HMAC-SHA256 защиты
- Тесты обработки ошибок ввода
- Тесты UTF-8 валидации
- Тесты path traversal защиты
- Тесты безопасной конвертации времени
- Тесты проверки границ коллизий
- Тесты валидации путей с кэшированием
- Тесты оптимизации SmallVec для линий
- Тесты валидации Unicode (эмодзи, bidi-символы)
- Тесты централизации констант
- Тесты #[must_use] и #[deprecated] атрибутов
- Тесты обработки Direction::Down и NoRotation
- Тесты константы ANONYMOUS_NAME
- Тесты deprecated методов GameState
- Тесты HMAC ключей
- Тесты инкапсуляции scoring модуля

**ВСЕГО: 1113 тестов** (unit + integration + architecture)

### 🏗️ Архитектурные улучшения (версия 23.96.15+)

**Разделение модуля menu:**
- Удалён дублирующий файл `src/menu.rs`
- Создан `src/menu/mod.rs` с переэкспортом функций
- Улучшена модульная структура

**Централизация констант:**
- Константы переэкспортируются из `game/constants.rs`
- Удалены дублирующие определения
- Соблюдение принципа DRY

**Разделение трейта GameBoardAccess:**
- Создан трейт `BoardReadonly` (только чтение)
- Создан трейт `BoardMutable` (чтение и запись)
- Улучшена инкапсуляция данных

**Добавление методов отрисовки в GameView:**
- Добавлены методы `draw_field()`, `draw_shape()`, `draw_ui()`
- Уменьшено использование полей GameState из render.rs
- Улучшена абстракция отрисовки

**Новые тесты архитектуры:**
- Добавлено 16 тестов на архитектурные ограничения
- Проверка отсутствия циклических зависимостей
- Проверка соблюдения границ модулей
- **Всего тестов: 1086** (все проходят)

## 🆕 Новые функции (версия 23.96.11)

### 🏆 Таблица лидеров с rate limiting

**Защита от спама рекордов:**
- Ограничение: 10 записей в минуту (cooldown 6 секунд)
- Автоматическая защита от DoS-атак через спам рекордов
- Отслеживание времени последней записи

### ⚡ Оптимизации производительности

**Dirty rectangle tracking:**
- Отрисовка только изменённых областей игрового поля
- Уменьшение количества операций вывода в терминал
- Повышение FPS в критических ситуациях

**Битовая маска для строк:**
- Использование `u32` вместо `[bool; GRID_HEIGHT]` для проверки заполненных линий
- Уменьшение использования памяти с 20 байт до 4 байт
- Ускорение проверки заполненных линий

**Массив на стеке:**
- Замена `Box<[[i8; GRID_WIDTH]; GRID_HEIGHT]>` на `[[i8; GRID_WIDTH]; GRID_HEIGHT]`
- Размещение игрового поля на стеке вместо кучи
- Ускорение доступа к ячейкам поля

**Оптимизация работы со строками:**
- Использование `String::with_capacity()` + `write!()` вместо `format!()`
- Уменьшение аллокаций памяти при форматировании

**Оптимизация Bag Generator:**
- Использование `reserve()` + `extend_from_slice()` в методе `fill_bag()`
- Уменьшение количества реаллокаций вектора

### 🔧 Изменения в API

**BLAKE3 вместо ручного кодирования:**
- Использование `blake3::hash()` для хеширования рекордов
- Криптографически стойкая хеш-функция
- Замена ручного hex-кодирования на библиотечную функцию

### 🧪 Новые тесты

**test_all_fixes.rs (54 теста):**
- Комплексное тестирование всех исправлений и улучшений
- Проверка атрибутов `#[must_use]`
- Тесты оптимизаций производительности
- Валидация документации API

## 📖 Стратегии игры

### Советы для новичков

1. **Держите поле плоским** — избегайте высоких столбцов
2. **Оставляйте колодец** — оставьте одну колонку пустой для фигуры-линии (I)
3. **Планируйте наперёд** — смотрите на следующую фигуру
4. **Используйте вращение** — некоторые позиции доступны только через вращение
5. **Используйте Hold** — откладывайте ненужные фигуры для будущих комбинаций

### Продвинутые техники

#### T-Spin
Вращение T-фигуры в последнюю минуту для размещения в узких местах. Позволяет набирать больше очков.

#### Комбо
Постоянное удаление линий создаёт цепную реакцию очков. Старайтесь оставлять несколько линий для последовательного удаления.

#### Удержание центра
Старайтесь держать самые высокие блоки в центре поля для большей гибкости.

#### Использование призрачной фигуры
Призрачная фигура (полупрозрачная) показывает, куда упадет текущая фигура. Используйте это для точного позиционирования.

## 🏗️ Архитектура

### Обзор

Tetris CLI использует модульную архитектуру с чётким разделением ответственности между компонентами.

**Версия проекта:** 23.96.21+
**Количество тестов:** 1500+
**Общая оценка архитектуры:** 8/10
**Подробнее:** [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)

### Ключевые компоненты

#### Модульная структура

Проект разделён на независимые модули:
- **`app/`** — application layer, управление жизненным циклом
- **`game/`** — игровая логика (state, logic/, scoring/, render/)
- **`menu/`** — главное меню
- **`highscore/`** — система рекордов с защитой от подделки
- **`tetromino/`** — фигуры и 7-bag система
- **`controls/`** — конфигурация управления
- **`validation/`** — валидация данных и путей
- **`crypto/`** — криптография (HmacValidator, BLAKE3)
- **`io/`** — ввод/вывод в терминал

#### HmacValidator (v23.96.20+)

Новый компонент для работы с HMAC-SHA256 подписями:
- **Constant-time сравнение** — защита от timing-атак
- **Подпись и проверка** — методы `sign()`, `verify()`, `verify_and_return()`
- **Случайные ключи** — генерация через OS RNG
- **Применение** — защита конфигурационных файлов, проверка целостности данных

#### Разделение ответственности

- **logic/** — игровая логика (input, physics, collision, rotation, update)
- **scoring/** — система очков (lines, points, combo)
- **render/** — отрисовка игрового поля и UI

### Принципы проектирования

- **SOLID** — Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion
- **DRY** — централизованная валидация, общие константы
- **KISS** — простые структуры данных, минимум абстракций
- **YAGNI** — удалены неиспользуемые зависимости и поля

### Диаграмма архитектуры

```
┌─────────────────────────────────────────────────────────────┐
│                        main.rs                              │
│  • Точка входа в приложение                                 │
│  • Отображение главного меню                                │
│  • Инициализация Canvas и KeyReader                         │
│  • Управление циклом меню и запуском игры                   │
│  • Таблица лидеров (топ-5)                                  │
│  • Выбор режима игры (Классика/Спринт/Марафон)              │
│  • Ввод имени игрока                                        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        game.rs                              │
│  • Основной игровой цикл (GameState)                        │
│  • Обработка ввода во время игры                            │
│  • Логика падения фигур                                     │
│  • Проверка и удаление заполненных линий                    │
│  • Отрисовка игрового поля                                  │
│  • Система очков и уровней                                  │
│  • Призрачная фигура (точка приземления)                    │
│  • Предпросмотр следующей фигуры                            │
│  • Удержание фигуры (Hold)                                  │
│  • Статистика игры (GameStats)                              │
│  • Режимы игры (Classic/Sprint/Marathon)                    │
└─────────────────────────────────────────────────────────────┘
                              │
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
┌───────────────────┐ ┌───────────────────┐ ┌───────────────────┐
│   tetromino.rs    │ │      io.rs        │ │   highscore.rs    │
│                   │ │                   │ │                   │
│ • Типы фигур      │ │ • Canvas          │ │ • SaveData        │
│ • Координаты      │ │ • Отрисовка       │ │ • Leaderboard     │
│ • Вращение        │ │ • Ввод            │ │ • Загрузка        │
│ • Цвета           │ │ • Терминал        │ │ • Сохранение      │
│ • Bag Generator   │ │                   │ │ • Хеширование     │
│ • 25 тестов       │ │ • 10 тестов       │ │ • 15 тестов       │
└───────────────────┘ └───────────────────┘ └───────────────────┘
                              │
                              ▼
                    ┌───────────────────┐
                    │   controls.rs     │
                    │                   │
                    │ • ControlsConfig  │
                    │ • Настройки       │
                    │ • Валидация       │
                    │ • 20 тестов       │
                    └───────────────────┘
```

### Модули

#### main.rs

**Назначение**: Точка входа в приложение и управление состоянием меню.

**Основные компоненты**:
- `MENU` — массив строк для отображения главного меню
- `LEADERBOARD_MENU` — меню таблицы лидеров
- `main()` — инициализация терминала, загрузка рекорда, цикл меню
- `get_player_name()` — запрос имени для таблицы лидеров
- `show_leaderboard()` — отображение топ-5 рекордов
- `show_game_stats()` — показ статистики после игры

**Поток выполнения**:
1. Загрузка рекорда из конфигурации
2. Загрузка и валидация таблицы лидеров
3. Проверка размера терминала
4. Инициализация Canvas и KeyReader
5. Цикл отрисовки меню (60 FPS)
6. Обработка нажатий:
   - `Enter` — запуск классической игры
   - `R` — запуск режима спринт
   - `M` — запуск режима марафон
   - `L` — отображение таблицы лидеров
   - `Backspace` — выход
7. После игры: отображение статистики, сохранение рекорда

#### game.rs

**Назначение**: Реализация игровой логики и цикла игры.

**Основные компоненты**:
- `GameState` — структура состояния игры
  - `score` — текущий счёт
  - `level` — уровень
  - `lines_cleared` — количество удалённых линий
  - `curr_shape` — текущая фигура
  - `next_shape` — следующая фигура
  - `held_shape` — удержанная фигура
  - `can_hold` — флаг возможности удержания
  - `fall_spd` — скорость падения
  - `blocks` — сетка игрового поля (10×20)
  - `land_timer` — таймер приземления фигуры
  - `stats` — статистика игры
  - `mode` — режим игры
- `GameStats` — статистика игры
- `GameMode` — режимы игры (Classic/Sprint/Marathon)
- `Dir` — направления движения (Down, Left, Right)
- `UpdateEndState` — состояния завершения обновления

**Игровой цикл**:
1. Поддержание стабильного FPS (60)
2. Обработка ввода пользователя
3. Обновление состояния фигуры
4. Проверка приземления
5. Отрисовка кадра

**Методы GameState**:
- `new()` — создание нового состояния игры (классический режим)
- `new_sprint()` — создание состояния для режима спринт
- `new_marathon()` — создание состояния для режима марафон
- `play()` — запуск игрового цикла
- `update()` — обновление состояния за один кадр
- `save_tetromino()` — фиксация фигуры в сетке
- `check_rows()` — проверка и удаление заполненных линий
- `draw()` — отрисовка текущего состояния
- `can_move_curr_shape()` — проверка возможности движения
- `can_rotate_curr_shape()` — проверка возможности вращения
- `hold_shape()` — удержание фигуры

#### tetromino.rs

**Назначение**: Определение типов фигур и их поведения.

**Типы фигур (ShapeType)**:

| Тип | Название | Цвет | Координаты |
|-----|----------|------|------------|
| T | T-образная | Пурпурный (Magenta) | (-1,0), (0,0), (1,0), (0,1) |
| L | L-образная | Жёлтый (Yellow) | (-1,-1), (0,-1), (0,0), (0,1) |
| J | J-образная | Синий (Blue) | (1,-1), (0,-1), (0,0), (0,1) |
| S | S-образная | Зелёный (Green) | (0,-1), (0,0), (1,0), (1,1) |
| Z | Z-образная | Светло-красный (LightRed) | (0,-1), (0,0), (-1,0), (-1,1) |
| O | Квадрат | Светло-жёлтый (LightYellow) | (0,0), (1,0), (0,1), (1,1) |
| I | Линия | Голубой (Cyan) | (0,-1), (0,0), (0,1), (0,2) |

**Bag Generator**:
- Система 7-bag гарантирует равномерное распределение фигур
- Каждые 7 фигур содержат все 7 типов
- Использует алгоритм Fisher-Yates для перемешивания

**Методы Tetromino**:
- `select()` — случайный выбор новой фигуры
- `from_bag()` — создание фигуры из Bag Generator
- `rotate()` — вращение фигуры на 90°

#### io.rs

**Назначение**: Абстракция ввода-вывода для терминала.

**Константы**:
- `SHAPE_STR` = "██" — строковое представление блока
- `SHAPE_WIDTH` = 2 — ширина блока в символах
- `GRID_WIDTH` = 10 — ширина игрового поля
- `GRID_HEIGHT` = 20 — высота игрового поля
- `DISP_WIDTH` = 22 — полная ширина дисплея
- `DISP_HEIGHT` = 25 — полная высота дисплея

**Структуры**:
- `Canvas` — обёртка над RawTerminal для отрисовки
  - `new()` — инициализация терминала
  - `reset()` — сброс терминала
  - `draw_strs()` — отрисовка статических строк
  - `draw_string()` — отрисовка динамической строки
  - `flush()` — обновление экрана
- `KeyReader` — асинхронный читатель клавиатуры
  - `new()` — создание читателя
  - `get_key()` — получение кода клавиши

#### highscore.rs

**Назначение**: Сохранение и загрузка рекордов с защитой от подделки.

**Структура SaveData**:
- `high_score` — значение рекорда
- `high_score_salt` — случайная соль для хеширования
- `high_score_hash` — хеш рекорда с солью

**Структура LeaderboardEntry**:
- `name` — имя игрока
- `score` — значение рекорда
- `salt` — случайная соль
- `hash` — хеш записи

**Структура Leaderboard**:
- `entries` — вектор записей (максимум 5)

**Методы**:
- `load_config()` — загрузка из файла конфигурации
- `from_value()` — создание новой записи с хешем
- `save_value()` — сохранение рекорда
- `assert_hs()` — проверка целостности рекорда
- `get_random_hash()` — генерация случайной соли
- `get_hash()` — вычисление хеша строки (BLAKE3)
- `add_score()` — добавление рекорда в таблицу
- `validate()` — валидация записей
- `save()` — сохранение таблицы лидеров

**Защита от подделки**:
Рекорд сохраняется вместе с криптографической солью и хешем. При загрузке хеш пересчитывается и сравнивается с сохранённым. Если хеши не совпадают, рекорд сбрасывается в 0.

#### controls.rs

**Назначение**: Конфигурация управления игрой.

**Структура ControlsConfig**:
- `move_left` — движение влево (по умолчанию 'a')
- `move_right` — движение вправо (по умолчанию 'd')
- `soft_drop` — мягкое падение (по умолчанию 's')
- `hard_drop` — жёсткое падение (по умолчанию 'w')
- `rotate_left` — вращение влево (по умолчанию 'q')
- `rotate_right` — вращение вправо (по умолчанию 'e')
- `hold` — удержание фигуры (по умолчанию 'c')
- `pause` — пауза (по умолчанию 'p')
- `quit` — выход (по умолчанию Backspace)

**Новые функции (версия 23.96.16+)**:
- `#[derive(Eq)]` — добавлено для сравнения конфигураций
- `#[must_use]` — атрибуты для методов, результат которых должен быть использован
- `const fn` — константные функции для инициализации конфигурации

**Методы**:
- `default_config()` — конфигурация по умолчанию
- `custom()` — создание кастомной конфигурации
- `validate()` — проверка на дубликаты и недопустимые значения
- `save_to_file()` — сохранение в JSON файл
- `load_from_file()` — загрузка из JSON файла

### Поток данных

```
Пользователь
    │
    ▼
### Игровой цикл (детали)

#### Частота обновления
- **FPS**: 60 кадров в секунду
- **Интервал кадра**: ~16.67 мс

#### Физика падения
- Начальная скорость: 0.9 блоков/секунду
- Прирост скорости: +0.05 за каждую заполненную линию
- Плавное движение с интерполяцией по времени

#### Таймер приземления
- Задержка после касания: 0.1 секунды
- Даёт время на перемещение фигуры перед фиксацией

## 🔧 Сборка

### Требования

- Rust 1.56 или новее
- Cargo

### Команды сборки

```bash
# Отладочная сборка
cargo build

# Релизная сборка
cargo build --release

# Запуск
cargo run

# Запуск тестов
cargo test

# Запуск бенчмарков
cargo bench

# Проверка кода
cargo clippy

# Форматирование кода
cargo fmt
```

## 📁 Структура проекта

```
tetris-cli/
├── src/
│   ├── main.rs          # Точка входа (делегирование app::run)
│   ├── app/             # Application layer (новая архитектура)
│   │   ├── mod.rs
│   │   ├── application.rs  # Application struct, игровой цикл
│   │   └── menu_loop.rs    # Логика меню
│   ├── menu/            # Главное меню (разделение ответственности)
│   │   ├── mod.rs
│   │   ├── constants.rs    # Константы меню (MENU, MENU_COLOR)
│   │   ├── draw.rs         # Функции отрисовки (draw_menu, draw_leaderboard)
│   │   └── input.rs        # Обработка ввода (get_player_name, wait_for_key)
│   ├── menu.rs          # Ре-экспорт для обратной совместимости
│   ├── game/            # Игровой модуль
│   │   ├── mod.rs
│   │   ├── state.rs     # GameState, GameStats, GameMode
│   │   ├── mode_trait.rs     # GameModeTrait (расширяемые режимы)
│   │   ├── modes.rs          # ClassicMode, SprintMode, MarathonMode
│   │   ├── logic.rs     # Физика, коллизии, ввод
│   │   ├── scoring.rs   # Очки, линии, уровни
│   │   ├── render.rs    # Отрисовка, анимации
│   │   ├── cycle.rs     # Игровой цикл, FPS
│   │   ├── view.rs      # GameView для отрисовки
│   │   ├── access.rs    # Трейты доступа
│   │   └── cache.rs     # Кэширование строк
│   ├── highscore/       # Система рекордов (новая архитектура)
│   │   ├── mod.rs
│   │   ├── save_data.rs    # SaveData
│   │   ├── leaderboard.rs  # Leaderboard, LeaderboardEntry
│   │   └── sanitize.rs     # sanitize_player_name
│   ├── highscore.rs     # Ре-экспорт (обратная совместимость)
│   ├── tetromino.rs     # Фигуры, вращение, Bag Generator
│   ├── io.rs            # Ввод/вывод, терминал
│   ├── io_traits.rs     # Трейты InputReader, Renderer
│   ├── controls.rs      # Конфигурация управления
│   ├── crypto.rs        # Криптографические утилиты
│   ├── exports.rs       # Публичные ре-экспорты
│   ├── types.rs         # Direction, RotationDirection
│   ├── terminal_backend.rs  # TerminalBackend трейт (кроссплатформенность)
│   ├── validation/      # Валидация данных
│   │   ├── mod.rs
│   │   ├── name.rs         # Валидация имён
│   │   └── path.rs         # Валидация путей
│   └── testes/          # Интеграционные тесты (60+ файлов)
├── docs/
│   ├── ARCHITECTURE.md  # Архитектура проекта
│   ├── SECURITY.md      # Безопасность (хеширование, валидация)
│   └── img/
│       └── screenshot.png
├── TODO.md              # План будущих улучшений
├── Cargo.toml           # Зависимости проекта
├── LICENSE              # Лицензия GPL-3.0
├── CHANGELOG.md         # История изменений
└── README.md            # Этот файл
```

## 🧪 Тестирование

Проект содержит **1085 модульных и интеграционных тестов** (все проходят успешно).

### Группы тестов

1. **Tetromino (25 тестов)**: создание фигур, вращение, координаты, Bag Generator
2. **Game Logic (30 тестов)**: движение, столкновения, вращение, система очков, уровни
3. **Highscore (15 тестов)**: SaveData, Leaderboard, хеширование, валидация
4. **IO (10 тестов)**: Canvas, KeyReader, константы размеров
5. **Controls (20 тестов)**: конфигурация управления, валидация, сохранение/загрузка
6. **Integration (20 тестов)**: взаимодействие компонентов, производительность
7. **Physics (20 тестов)**: физическая механика, гравитация, столкновения, вращение, hold, bag generator
8. **Game Extended (100 тестов)**: расширенные тесты игровой логики
9. **Tetromino Extended (80 тестов)**: расширенные тесты фигур
10. **Scoring (50 тестов)**: система очков
11. **Collision (50 тестов)**: столкновения
12. **Animation (30 тестов)**: анимации
13. **Modes (40 тестов)**: режимы игры
14. **Statistics (30 тестов)**: статистика
15. **Edge Cases (50 тестов)**: граничные случаи
16. **Edge Cases Stress (20 тестов)**: стресс-тесты
17. **Game Movement (50 тестов)**: движение фигур
18. **Game Rotation (50 тестов)**: вращение фигур
19. **Tetromino Shapes (40 тестов)**: формы фигур
20. **Bag System (30 тестов)**: Bag Generator
21. **Scoring Detailed (40 тестов)**: детальная система очков
22. **Game Modes Detailed (40 тестов)**: детальное тестирование режимов
23. **Highscore Detailed (30 тестов)**: детальное тестирование рекордов
24. **Controls Detailed (25 тестов)**: детальное тестирование управления
25. **IO Detailed (20 тестов)**: детальное тестирование ввода-вывода
26. **Integration Extended (50 тестов)**: расширенные интеграционные тесты
27. **Highscore U256 Fix (3 теста)**: тесты конвертации байтов в hex
28. **Game Bounds Check (3 теста)**: тесты проверок границ
29. **Highscore Rate Limiting (3 теста)**: тесты rate limiting
30. **Controls Path Validation (3 теста)**: тесты валидации путей
31. **Game Wall Kick (3 теста)**: тесты wall kick константы
32. **Highscore Integrity (3 теста)**: тесты целостности данных
33. **Test Fixes Comprehensive (36 тестов)**: комплексные тесты исправлений и улучшений
34. **Game Box Array (3 теста)**: тесты массива на стеке
35. **IO UTF-8 Handling (5 тестов)**: тесты обработки UTF-8
36. **Highscore Error Handling (5 тестов)**: тесты обработки ошибок рекордов
37. **Highscore Verify Integrity (5 тестов)**: тесты проверки целостности рекордов
38. **Tetromino Dir Down (5 тестов)**: тесты направления Down
39. **Game Score Overflow (5 тестов)**: тесты переполнения счёта
40. **Highscore Random Hash (5 тестов)**: тесты случайного хеширования
41. **Game Bitmask Check Rows (5 тестов)**: тесты битовой маски check_rows
42. **Unwrap to Expect (5 тестов)**: тесты замены unwrap на expect
43. **Error Propagation (5 тестов)**: тесты распространения ошибок
44. **Benchmarks (5 тестов)**: тесты производительности
45. **Highscore Deprecated Assert HS (5 тестов)**: тесты замены assert_hs() на verify_and_get_score()
46. **Game Rotation Bounds (5 тестов)**: тесты границ вращения фигур
47. **Controls Path Traversal (5 тестов)**: тесты защиты от Path Traversal атак
48. **Game Score Overflow Protection (6 тестов)**: тесты защиты от переполнения счёта
49. **Fixes Must Use Stack Format (12 тестов)**: тесты атрибутов #[must_use] и оптимизаций
50. **Fixes Bag Preview Rotate (13 тестов)**: тесты Bag Generator и вращения
51. **Fixes Documentation Validation (24 теста)**: тесты документации и валидации
52. **Fixes Final Issues (19 тестов)**: финальные тесты исправлений
53. **Test All Fixes (54 теста)**: комплексные тесты всех исправлений и улучшений
54. **Fixes Verification Extended (39 тестов)**: расширенные тесты верификации исправлений
55. **Test Audit Fixes (13 тестов)**: тесты исправлений аудита кода
56. **Test 32 Fixes Comprehensive (33 теста)**: комплексные тесты для всех 32 исправлений (критические ошибки, логические ошибки, оптимизации, читаемость, обработка ошибок, безопасность, best practices)
57. **Cast Safety (12 тестов)**: тесты безопасности cast операций (usize → i16, f32 → u32, защита от NaN/infinity, обработка ошибок)
58. **Test All Fixes (141 тест)**: комплексные тесты всех исправлений и улучшений (экспорт GameStats, оптимизация sanitize_player_name, кэширование строк, обработка Direction::Down, проверки границ, валидация Unicode, #[must_use] атрибуты, #[track_caller])
59. **Refactoring Fixes (55 тестов)**: тесты рефакторинга (инкапсуляция GameState, race condition fix, match вместо if let, assert вместо debug_assert)
60. **Architecture Constraints (20+ тестов)**: тесты на отсутствие циклических зависимостей, соблюдение границ модулей, инкапсуляцию, использование трейтов
61. **Audit Fixes Comprehensive (25 тестов)**: комплексные тесты всех исправлений аудита (Critical, High, Medium, Low)
62. **Audit 2026-03-28 (18 тестов)**: тесты исправлений аудита от 28 марта 2026 (документация, similar_names, cast safety, format_args, refactoring)

### Запуск тестов

```bash
cargo test
```

### Статистика тестов

```
═══════════════════════════
ВСЕГО: 1113 тестов
ВСЕ ПРОХОДЯТ: ✅ (100%)
═══════════════════════════
```

### Покрытие тестов

Тесты покрывают все основные компоненты:
- ✅ Создание и валидация конфигурации управления
- ✅ Игровая логика и физика
- ✅ Система рекордов и таблица лидеров
- ✅ Ввод/вывод в терминале
- ✅ Фигуры и их поведение
- ✅ Интеграция всех компонентов
- ✅ Производительность операций
- ✅ Безопасность cast операций (usize → i16, f32 → u32)
- ✅ Обработка ошибок и их распространение
- ✅ Защита от переполнения при конвертации типов
- ✅ Архитектурная целостность (отсутствие циклических зависимостей, границы модулей)
- ✅ Terminal Backend абстракция (Mock-объекты для тестирования)
- ✅ UTF-8 ограничения и обработка Unicode
- ✅ Разделение menu.rs на подмодули (constants, draw, input)
- ✅ Комплексные тесты исправлений аудита (Critical, High, Medium, Low приоритеты)
- ✅ **Constant-time HMAC-SHA256** — защита от timing-атак при проверке подписей
- ✅ **Безопасное вращение фигур** — rotate() не паникует при выходе за границы
- ✅ **Path traversal защита** — canonicalize() перед проверкой путей
- ✅ **Валидация UTF-8** — отбрасывание невалидных последовательностей
- ✅ **Оптимизация sanitize_player_name** — фильтрация в один проход
- ✅ **Защита от переполнения времени** — безопасная конвертация u128 → u64

## 📊 Зависимости

| Зависимость | Версия | Назначение |
|-------------|--------|------------|
| termion | 2.0 | Работа с терминалом (raw-режим, цвета, курсор) |
| rand | 0.8.4 | Генерация случайных фигур |
| confy | 0.4.0 | Управление конфигурацией (сохранение рекордов) |
| serde | 1.0 | Сериализация/десериализация SaveData |
| serde_json | 1.0 | Работа с JSON для ControlsConfig |
| blake3 | 1.5 | Криптографическое хеширование для защиты рекордов |

**Dev-dependencies:**
| Зависимость | Версия | Назначение |
|-------------|--------|------------|
| tempfile | 3.10 | Временные файлы для тестирования |

## 📝 Лицензия

Этот проект распространяется под лицензией **GPL-3.0** (GNU General Public License v3.0).

Это означает, что вы можете:
- ✅ Использовать код в личных целях
- ✅ Модифицировать исходный код
- ✅ Распространять модификации

При условии:
- 📢 Сохранение лицензии в производных работах
- 🔓 Открытость модификаций
- 📄 Предоставление исходного кода

## 👥 Авторы

- **Dylan Turner** — оригинальная реализация (около 2022 г.)
- Дополнительные участники (см. историю коммитов)

## 🏆 История изменений

Подробная история изменений доступна в файле [CHANGELOG.md](CHANGELOG.md).

### Текущая версия: 23.96.23 (2026-03-30)

**Новые функции:**
- **Улучшенная обработка ошибок ввода** — `KeyReader::get_key()` возвращает `io::Result<Option<u8>>`
- **RotationDirection::NoRotation** — новый вариант для представления отсутствия вращения
- **Константа ANONYMOUS_NAME** — централизованная константа для имени по умолчанию
- **Улучшенная документация Leaderboard** — явное документирование `!Send + !Sync`

**Исправлено:**
- **Обработка ошибок ввода** — корректная обработка через `io::Result`
- **Типизация направлений** — улучшенная типизация вращения
- **Документация потокобезопасности** — явная документация о `!Send + !Sync`

**Улучшено:**
- **Обработка ошибок** — корректная обработка ошибок ввода через систему типов
- **Типизация** — улучшенная типизация направлений вращения
- **Документация** — явная документация о потокобезопасности Leaderboard

**Тестирование:**
- **1113 модульных и интеграционных тестов** (все проходят успешно)
- **0 предупреждений компиляции**
- **Добавлено 23 новых теста** — обработка ошибок, Direction, валидация, deprecated методы, HMAC, инкапсуляция

---

### Версия 23.96.22 (2026-03-28)

**Новые функции:**
- **Constant-time HMAC-SHA256** — защита от timing-атак при проверке криптографических подписей
- **Безопасное вращение фигур** — rotate() не паникует при выходе координат за границы
- **Path traversal защита** — canonicalize() выполняется перед проверкой путей
- **Валидация UTF-8** — корректное отбрасывание невалидных последовательностей
- **Оптимизация sanitize_player_name** — фильтрация имён в один проход
- **Защита от переполнения времени** — безопасная конвертация u128 → u64

**Исправлено:**
- **Переполнение при конвертации времени** — безопасная конвертация системного времени
- **Timing-атака в HMAC** — constant-time comparison для предотвращения атак по времени
- **Валидация UTF-8** — корректная обработка многобайтовых символов
- **Безопасное вращение** — rotate() использует saturating операции вместо паники
- **Path traversal защита** — canonicalize() перед проверкой нахождения в директории
- **Оптимизация sanitize_player_name** — объединение фильтров в один проход

**Улучшено:**
- **Безопасность** — constant-time comparison, UTF-8 валидация, path traversal защита
- **Производительность** — оптимизация sanitize_player_name, безопасное вращение
- **Надёжность** — защита от переполнения времени, безопасное вращение без паники

**Тестирование:**
- **1085 модульных и интеграционных тестов** (все проходят успешно)
- **0 предупреждений компиляции**
- **Добавлены тесты безопасности** — constant-time HMAC, UTF-8 валидация, path traversal

---

### Версия 23.96.23 (2026-03-23)

**Исправлено:**
- **17 проблем из аудита кода** — полный аудит и исправление всех критических, логических ошибок и проблем производительности
- **Бенчмарки** — исправлен `criterion_main!`, добавлена явная функция `main()`
- **SystemTime rate limiting** — реализована защита от изменения системного времени назад
- **Множественные `format!()` аллокации** — добавлено кэширование строк в цикле отрисовки
- **`update()` функция** — разбита на подфункции: `handle_input()`, `handle_falling()`, `handle_landing()`
- **Магические числа** — добавлена константа `LEVEL_BONUS_MULT = 500`
- **`validate_config_path()`** — разбита на 4 подфункции валидации
- **Обработка ошибок** — унифицировано: production код возвращает `Result`, тесты используют `expect()`
- **`#[must_use]` атрибуты** — добавлены ко всем методам, возвращающим `Result`/`bool`
- **Логирование ошибок** — добавлено логирование через `eprintln!`
- **Контекст ошибок** — добавлен контекст к `IoError`
- **Документация** — добавлены `///` комментарии ко всем `pub` методам
- **rustdoc предупреждения** — исправлены ссылки на private элементы

**Добавлено:**
- **Структура `RateLimitState`** — для защиты от обхода rate limiting
- **Функция `get_current_time_ms_protected()`** — защита от изменения времени назад
- **Кэширование строк в `GameState`** — снижение аллокаций с 180/сек до ~1/сек
- **Константа `LEVEL_BONUS_MULT`** — для бонуса за повышение уровня
- **4 подфункции валидации путей** — `validate_path_length()`, `validate_path_characters()`, `validate_no_symlinks()`, `validate_path_within_directory()`
- **23 новых теста** в `test_audit_fixes_verification.rs` — комплексное тестирование всех исправлений

**Улучшено:**
- **Производительность отрисовки** — кэширование строк снижает количество аллокаций
- **Безопасность rate limiting** — защита от изменения системного времени
- **Читаемость кода** — разбиты большие функции на подфункции
- **Консистентность обработки ошибок** — унифицирован подход
- **Документация API** — все публичные методы задокументированы

**Тестирование:**
- **1533 модульных и интеграционных тестов** (все проходят успешно)
- **0 предупреждений компиляции**
- **Бенчмарки компилируются и запускаются**

---

### Версия 23.96.18 (2026-03-20)

**Добавлено:**
- Константа `FRAME_DELAY_MS` для устранения дублирования кода
- Документация для контрибьюторов (`CONTRIBUTING.md`)
- Политика безопасности (`SECURITY.md`)
- Валидация имён в таблице лидеров (trim, фильтрация символов, ограничение длины, fallback на `Anonymous`)

**Исправлено:**
- Улучшена криптографическая генерация соли в `get_random_hash()` (используется OS RNG)
- Вынесена санитизация имени игрока в отдельную функцию
- Тест `test_long_name_truncated` (учёт UTF-8 символов)
- Улучшена обработка ошибок в `Canvas`

**Технические изменения:**
- Вынесена общая константа задержки кадров (16 мс)
- Рефакторинг обработки ошибок терминала
- Удалено дублирование кода

**Безопасность:**
- Фильтрация потенциально опасных символов в именах
- Гарантированная длина хеша 64 символа
- Улучшена защита таблицы лидеров

**Тестирование:**
- 1548 модульных и интеграционных тестов
- Все тесты проходят успешно (3 пропущено)
- 0 предупреждений clippy

---

### Версия 23.96.5 (2026-03-17)

**Добавлено:**
- Исправления предупреждений компилятора
- Улучшение читаемости кода
- Вспомогательная функция `Canvas::exit_with_terminal_reset()`

**Исправлено:**
- Предупреждения `unused_mut` в тестах (12 мест)
- Приведение типов в `test_bag_system.rs`
- Улучшена обработка ошибок в `Canvas`

**Технические изменения:**
- Убраны лишние `mut` в тестах
- Заменено на `unsigned_abs()` где требовалось
- Улучшена обработка ошибок терминала

**Тестирование:**
- 1599 модульных и интеграционных тестов
- Все тесты проходят успешно

---

### Версия 23.96.4 (2026-03-14)

**Добавлено:**
- Удержание фигуры (Hold) с отображением
- Режим спринт (40 линий на время)
- Режим марафон (150 линий)
- Звуковые эффекты (терминальный bell)
- Расширенная статистика игры
- Экран статистики после завершения игры

**Исправлено:**
- Debug для GameMode
- Геттеры для тестирования

**Тестирование:**
- 1548 модульных и интеграционных тестов
- Все тесты проходят успешно

## 🐛 Известные ограничения

1. **Только Linux/Unix**: Windows требует эмулятора терминала (WSL, Cygwin)
2. **Моноширинный шрифт**: требуется для корректного отображения
3. **Минимальный размер**: Окно терминала должно быть не менее 22×25
4. **ASCII-графика**: Ограниченная визуализация по сравнению с GUI

## 🔗 Ссылки

- [Репозиторий на GitHub](https://github.com/Githab-capibara/tetris-cli)
- [Документация](https://github.com/Githab-capibara/tetris-cli/tree/master/docs)
- [Архитектура](docs/ARCHITECTURE.md)
- [История изменений](CHANGELOG.md)
- [AUR Package](https://aur.archlinux.org/packages/tetris-cli-git)
- [NetBSD Package](https://pkgsrc.se/games/tetris-cli)

## 📞 Поддержка

Если вы столкнулись с проблемой:

1. Проверьте [README.md](README.md) для общей информации
2. Посмотрите [ARCHITECTURE.md](docs/ARCHITECTURE.md) для технических деталей
3. Изучите [CHANGELOG.md](CHANGELOG.md) для истории изменений
4. Создайте issue в репозитории GitHub

---

**Приятной игры!** 🎮
