//! Состояние игрового поля.
//!
//! # Ответственность
//! - Переэкспорт `GameBoard` для обратной совместимости
//!
//! ## Архитектурные заметки
//! Ранее содержал `BoardState` — тонкую обёртку над `GameBoard`,
//! которая не использовалась в production коде.
//! Теперь модуль содержит только реэкспорт `GameBoard`.
//!
//! ## Использование
//! Используйте `crate::game::board::GameBoard` напрямую —
//! этот реэкспорт сохранён для обратной совместимости.

// Re-export для обратной совместимости (не используется напрямую,
// но сохраняется для предотвращения breaking changes)
#[allow(unused_imports)]
pub use crate::game::board::GameBoard;
